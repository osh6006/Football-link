import ListBox from "components/common/list-box";

interface ITeamSelectorProps {}

const TeamSelector: React.FunctionComponent<ITeamSelectorProps> = (props) => {
  return <></>;
};

export default TeamSelector;
