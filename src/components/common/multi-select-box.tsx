import { createContext, useState } from "react";

interface IMulitSelectBoxProps {
  items: any[];
}

const MultiBoxContext = createContext(null);

const MulitSelectBox: React.FunctionComponent<IMulitSelectBoxProps> = ({}) => {
  return <></>;
};

export default MulitSelectBox;
